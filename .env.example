# Environment variables for CodeFlow application

# Database Configuration
DATABASE_URL="postgresql://codeflow:dev_password@localhost:5432/codeflow_dev"

# Docker Compose Database Variables (for local development)
POSTGRES_DB=codeflow_dev
POSTGRES_USER=codeflow
POSTGRES_PASSWORD=dev_password

# Application Environment
NODE_ENV=development
LOG_LEVEL=debug
